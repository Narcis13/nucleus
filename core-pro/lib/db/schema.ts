// TODO: Drizzle schema — tables + pgPolicy RLS. See SESSION 2.
// Source of truth for every table in the app. drizzle-kit generates SQL
// into `supabase/migrations/` so migrations flow through Supabase's system.
export {}
