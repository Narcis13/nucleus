<% content_for :page_title, "Edit #{@tag.name}" %>

<div class="space-y-6 max-w-md">
  <header>
    <h1 class="text-2xl font-semibold">Edit <%= @tag.name %></h1>
  </header>
  <%= render "form", tag: @tag %>
</div>
